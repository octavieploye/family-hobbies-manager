package com.familyhobbies.associationservice.entity.enums;

/**
 * Type of subscription to an activity.
 * Stored as VARCHAR in PostgreSQL for non-breaking additions.
 */
public enum SubscriptionType {
    ADHESION,
    COTISATION
}
