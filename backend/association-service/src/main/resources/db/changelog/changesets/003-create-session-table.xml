<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="003-create-session-table" author="family-hobbies-team">
        <comment>Creates t_session table for activity sessions/schedule (Sprint 3 - S3-001)</comment>

        <createTable tableName="t_session">
            <column name="id" type="BIGSERIAL" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="activity_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="day_of_week" type="VARCHAR(10)">
                <constraints nullable="false"/>
            </column>
            <column name="start_time" type="TIME">
                <constraints nullable="false"/>
            </column>
            <column name="end_time" type="TIME">
                <constraints nullable="false"/>
            </column>
            <column name="location" type="VARCHAR(200)"/>
            <column name="instructor_name" type="VARCHAR(100)"/>
            <column name="max_capacity" type="INTEGER"/>
            <column name="active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Foreign key to t_activity with CASCADE delete -->
        <addForeignKeyConstraint baseTableName="t_session"
                                 baseColumnNames="activity_id"
                                 referencedTableName="t_activity"
                                 referencedColumnNames="id"
                                 constraintName="fk_session_activity"
                                 onDelete="CASCADE"/>

        <!-- Indexes -->
        <createIndex tableName="t_session" indexName="idx_session_activity_id">
            <column name="activity_id"/>
        </createIndex>

        <createIndex tableName="t_session" indexName="idx_session_day_of_week">
            <column name="day_of_week"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
