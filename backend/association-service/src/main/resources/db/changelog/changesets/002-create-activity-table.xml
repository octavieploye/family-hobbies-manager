<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="002-create-activity-table" author="family-hobbies-team">
        <comment>Creates t_activity table for association activities (Sprint 3 - S3-001)</comment>

        <createTable tableName="t_activity">
            <column name="id" type="BIGSERIAL" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="association_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(200)">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="TEXT"/>
            <column name="category" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="level" type="VARCHAR(20)" defaultValue="ALL_LEVELS">
                <constraints nullable="false"/>
            </column>
            <column name="min_age" type="INTEGER"/>
            <column name="max_age" type="INTEGER"/>
            <column name="max_capacity" type="INTEGER"/>
            <column name="price_cents" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="season_start" type="DATE"/>
            <column name="season_end" type="DATE"/>
            <column name="status" type="VARCHAR(20)" defaultValue="ACTIVE">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Foreign key to t_association -->
        <addForeignKeyConstraint baseTableName="t_activity"
                                 baseColumnNames="association_id"
                                 referencedTableName="t_association"
                                 referencedColumnNames="id"
                                 constraintName="fk_activity_association"
                                 onDelete="CASCADE"/>

        <!-- Indexes for search performance -->
        <createIndex tableName="t_activity" indexName="idx_activity_association_id">
            <column name="association_id"/>
        </createIndex>

        <createIndex tableName="t_activity" indexName="idx_activity_category">
            <column name="category"/>
        </createIndex>

        <createIndex tableName="t_activity" indexName="idx_activity_status">
            <column name="status"/>
        </createIndex>

        <!-- Check constraints for enum values -->
        <addCheckConstraint tableName="t_activity" constraintName="chk_activity_level"
                            condition="level IN ('BEGINNER', 'INTERMEDIATE', 'ADVANCED', 'ALL_LEVELS')"/>

        <addCheckConstraint tableName="t_activity" constraintName="chk_activity_status"
                            condition="status IN ('ACTIVE', 'SUSPENDED', 'CANCELLED', 'COMPLETED')"/>

    </changeSet>

</databaseChangeLog>
